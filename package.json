{
  "name": "habit-game",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test shared/game-math.test.js shared/gesture-math.test.js shared/reminder-math.test.js shared/analytics.test.js",
    "check": "node --check app/www/app.js && node --check app/www/cloud.js && node --check app/www/fx.js && node --check app/www/store.js && node --check app/www/creature.js && node --check app/www/icons.js && node --check app/www/sheet.js && node --check app/www/habits.js && node --check app/www/screens.js && node --check app/www/onboarding.js && node --check app/www/audio.js && node --check app/www/reminders.js",
    "sync:shared": "node scripts/sync-shared.mjs",
    "build:firebase": "esbuild scripts/firebase-entry.js --bundle --format=esm --minify --outfile=app/www/vendor/firebase.js",
    "build": "npm run sync:shared && npm run build:firebase",
    "vercel-build": "node scripts/build-site.mjs",
    "serve": "npm run build && python -m http.server 4173 --directory app/www",
    "cap:sync": "npm run build && cap sync android",
    "test:rules": "firebase emulators:exec --only firestore \"node --test tests/rules/rules.test.js\""
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "@firebase/rules-unit-testing": "^5.0.0",
    "esbuild": "^0.28.1",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/local-notifications": "^8.2.1"
  }
}
