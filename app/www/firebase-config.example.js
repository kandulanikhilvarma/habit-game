// Copy to firebase-config.js (gitignored) once the Firebase project exists.
// These values are not secrets — Firestore rules are what protect the data.
export const firebaseConfig = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: '',
};
